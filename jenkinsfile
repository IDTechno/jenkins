pipeline {
  agent { label 'linux' }
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook -i mikrotik_host add_vpn.yml --extra-vars vpnname=$($env:name) --extra-vars  vpnpass=$($env:pass)'
      }
    }
  }
}