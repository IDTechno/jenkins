pipeline {
  agent { label 'linux' }
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-playbook -i inventory/mikrotik.host all -m ping'
        sh 'ansible -i inventory/mikrotik.host all -m ping'
      }
    }
  }
}